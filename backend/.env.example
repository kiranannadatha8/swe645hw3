# Copy this file to backend/.env and update the values for your environment.

# Environment can be local, development, or production
ENVIRONMENT=local

# Option A: provide a full SQLAlchemy-compatible URL (useful for quick local testing)
DATABASE_URL=mysql+pymysql://survey_user:survey_pass@your-rds-endpoint:3306/student_survey

# Option B: let the application compose the URL from discrete secrets (recommended for Kubernetes)
DB_DRIVER=mysql+pymysql
DB_HOST=your-rds-endpoint
DB_PORT=3306
DB_USER=survey_user
DB_PASSWORD=survey_pass
DB_NAME=student_surveys
DB_CHARSET=utf8mb4

# Comma-separated list or JSON array of allowed web origins
CORS_ORIGINS=http://localhost:5173,http://localhost:4173
