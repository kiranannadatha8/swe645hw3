apiVersion: v1
kind: Secret
metadata:
  name: survey-backend-secrets
  namespace: student-survey
type: Opaque
stringData:
  # Provide either the discrete DB_* secrets below or a single DATABASE_URL
  # DATABASE_URL: mysql+pymysql://survey_user:survey_pass@your-rds-endpoint:3306/student_survey
  DB_DRIVER: mysql+pymysql
  DB_HOST: student-surveys.ch4ezyusbvcp.us-east-1.rds.amazonaws.com
  DB_PORT: "3306"
  DB_USER: survey_user
  DB_PASSWORD: survey_password_2025
  DB_NAME: student_surveys
